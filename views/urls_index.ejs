<!DOCTYPE html>
<html lang="en">
  <head>
    <% include ./partials/_head %>
  </head>

  <header>
    <% include ./partials/_header %>
  </header>

  <body>
    <ul>
      <% for (let url in urls) { %>
      <li>
        <form method="POST" action="<% delete urls.url %>"><%= urls[url] %> (<%= url %>)
          <input type="submit" value="Delete">
        </form>
      </li>
      <% } %>
    </ul>
  </body>

  <footer>
    <% include ./partials/_footer %>
  </footer>
</html>
