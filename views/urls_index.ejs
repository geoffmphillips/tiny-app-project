<!DOCTYPE html>
<html lang="en">
  <head>
    <% include ./partials/_head %>
  </head>

  <header>
    <% include ./partials/_header %>
  </header>

  <body>
    <ul>
      <% for (let url in urls) { %>
        <form method="POST" action="/urls/<%= url %>/delete"><%= urls[url] %> &rarr; <%= url %> <a href="/urls/<%= url %>/">Edit</a>
          <input type="submit" value="Delete">
        </form>
      <% } %>
    </ul>
  </body>

  <footer>
    <a href="/urls/new">Shorten a new URL!</a>
    <% include ./partials/_footer %>
  </footer>
</html>
